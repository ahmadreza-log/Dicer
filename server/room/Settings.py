# Default settings applied when a Dungeon Master creates a room.


class Settings:
    # Who can see the room in listings: public | private.
    Visibility = "public"

    # Empty string means no password is required to join.
    Password = ""

    # Maximum players allowed in the room (excluding the DM).
    Capacity = 6
